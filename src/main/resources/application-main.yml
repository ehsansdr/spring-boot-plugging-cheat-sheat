#my.custom.proprties : "from profile dev"
#
#spring:
#  datasource:
#    driver-class-name: org.postgresql.Driver
#    url: jdbc:postgresql://localhost:5432/nnnn #(the name of database)
#    username: postgres # (the username that default is "postgres")
#    password: postgres # (the password that i set is "postgres")
#jpa:
#  hibernate:
#    ddl-auto: update
#  # by defulat this is false
#  how-sql: true    # when run or hibernate execute command or query  it will be displayed in your ide or in the logs of your application
#  properties:
#    hibernate:
#    format_sql: true
#  database: postgresql
#  database-platform: org.hibernate.dialect.PostgreSQLDialect

#spring:
#  datasource:
#    driver-class-name: org.postgresql.Driver
#    url: jdbc:postgresql://localhost:5432/ddd #(the name of database)
#    username: postgres # (the username that default is "postgres")
#    password: postgres # (the password that i set is "postgres")
#    jpa:
#      hibernate:
#        ddl-auto:   create-drop #update #
#      # by defulat this is false
#      show-sql: true    # when run or hibernate execute command or query  it will be displayed in your ide or in the logs of your application
#      properties:
#        hibernate:
#        format_sql: true
#      database: postgresql
#      database-platform: org.hibernate.dialect.PostgreSQLDialect
### The following 3 profiles are active: "dev", "fff", "ggg" even we do not create them
